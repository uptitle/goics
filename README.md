
### A go toolkit for decoding, encoding icalendar ics ical files

This is a form from https://github.com/jordic/goics

Examples are in the original repo. This fork removes the examples to have no other dependencies in go.mod, so they won't be pulled if using in a project.